[pytest]
asyncio_mode = auto
pythonpath = . scripts
asyncio_default_fixture_loop_scope = session
markers =
    unit: no network, no Qdrant, no live GPT
    live: needs local Qdrant and/or API keys; skip in default CI
    slow: long-running eval suites (GPT food parser, etc.)
